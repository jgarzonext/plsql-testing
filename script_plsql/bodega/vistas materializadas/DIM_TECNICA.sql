----- DELETE VISTA ----------
BEGIN
    PAC_SKIP_ORA.p_comprovadrop('DIM_TECNICA','MATERIALIZED VIEW');  
END;
/

----- CREATE VISTA ----------
  CREATE MATERIALIZED VIEW "CONF_DWH"."DIM_TECNICA" ("TEC_ID", "TEC_RAMO", "TEC_RAMO_DESC", "TEC_SUBRAMO", "TEC_SUBRAMO_DESC", "TEC_PRODUCTO", "TEC_SUBPRODUCTO", "TEC_SUBPRODUCTO_DESC", 
  "TEC_COBERTURA", "TEC_COBERTURA_DESC", "FECHA_REGISTRO", "ESTADO", "START_ESTADO", "END_ESTADO", "FECHA_CONTROL", "FECHA_INICIAL", "FECHA_FIN")
  ORGANIZATION HEAP PCTFREE 10 PCTUSED 40 INITRANS 1 MAXTRANS 255 
NOCOMPRESS LOGGING
STORAGE(INITIAL 65536 NEXT 1048576 MINEXTENTS 1 MAXEXTENTS 2147483645
PCTINCREASE 0 FREELISTS 1 FREELIST GROUPS 1
BUFFER_POOL DEFAULT FLASH_CACHE DEFAULT CELL_FLASH_CACHE DEFAULT)
TABLESPACE "CONF_DWH"   NO INMEMORY 
BUILD IMMEDIATE
USING INDEX 
REFRESH COMPLETE ON DEMAND START WITH SYSDATE+0 NEXT SYSDATE + 1
USING DEFAULT LOCAL ROLLBACK SEGMENT
USING ENFORCED CONSTRAINTS DISABLE QUERY REWRITE
  AS SELECT TEC_ID,
       TEC_RAMO,
       TEC_RAMO_DESC,
       TEC_SUBRAMO,
       TEC_SUBRAMO_DESC,
       TEC_PRODUCTO,
       TEC_SUBPRODUCTO,
       TEC_SUBPRODUCTO_DESC,
       TEC_COBERTURA,  
       TEC_COBERTURA_DESC, 
       FECHA_REGISTRO, 
       ESTADO,
       START_ESTADO, 
       END_ESTADO,
       FECHA_CONTROL, 
       FECHA_INICIAL,
       FECHA_FIN
       FROM DIM_TECNICA@BODEGA;

   COMMENT ON MATERIALIZED VIEW "CONF_DWH"."DIM_TECNICA"  IS 'SNAPSHOT TABLE FOR SNAPSHOT DWH_CONF.DIM_TECNICA';
