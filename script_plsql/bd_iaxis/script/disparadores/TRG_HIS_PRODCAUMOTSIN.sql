--------------------------------------------------------
--  DDL for Trigger TRG_HIS_PRODCAUMOTSIN
--------------------------------------------------------

  CREATE OR REPLACE EDITIONABLE TRIGGER "AXIS"."TRG_HIS_PRODCAUMOTSIN" 
   BEFORE UPDATE OR DELETE
   ON PRODCAUMOTSIN
   FOR EACH ROW
   DECLARE
       vaccion VARCHAR2(2);
BEGIN
   IF UPDATING THEN
       vaccion := 'U';
   ELSE
       vaccion := 'D';
   END IF;

      -- crear registro histórico
      INSERT INTO his_PRODCAUMOTSIN(
SPRCAMOSIN,
CRAMO,
CMODALI,
CTIPSEG,
CCOLECT,
CACTIVI,
CGARANT,
CCAUSIN,
CMOTSIN,
CMOVIMI,
CMOTMOV,
SPRODUC,
CMOTFIN,
CUSUALT,
FALTA,
CUSUMOD,
FMODIFI,
CUSUHIST,FCREAHIST,ACCION)
VALUES(
:OLD.SPRCAMOSIN,
:OLD.CRAMO,
:OLD.CMODALI,
:OLD.CTIPSEG,
:OLD.CCOLECT,
:OLD.CACTIVI,
:OLD.CGARANT,
:OLD.CCAUSIN,
:OLD.CMOTSIN,
:OLD.CMOVIMI,
:OLD.CMOTMOV,
:OLD.SPRODUC,
:OLD.CMOTFIN,
:OLD.CUSUALT,
:OLD.FALTA,
:OLD.CUSUMOD,
:OLD.FMODIFI,
f_user, f_sysdate, ''||vaccion||'');
EXCEPTION
   WHEN OTHERS THEN
      p_tab_error(f_sysdate, f_user, 'TRIGGER trg_his_PRODCAUMOTSIN', 1, SQLCODE, SQLERRM);
      RAISE;
END trg_his_PRODCAUMOTSIN;





/
ALTER TRIGGER "AXIS"."TRG_HIS_PRODCAUMOTSIN" ENABLE;
