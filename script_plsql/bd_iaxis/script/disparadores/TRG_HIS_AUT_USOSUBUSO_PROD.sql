--------------------------------------------------------
--  DDL for Trigger TRG_HIS_AUT_USOSUBUSO_PROD
--------------------------------------------------------

  CREATE OR REPLACE EDITIONABLE TRIGGER "AXIS"."TRG_HIS_AUT_USOSUBUSO_PROD" 
   BEFORE UPDATE OR DELETE
   ON AUT_USOSUBUSO_PROD
   FOR EACH ROW
   DECLARE
       vaccion VARCHAR2(2);
BEGIN
   IF UPDATING THEN
       vaccion := 'U';
   ELSE
       vaccion := 'D';
   END IF;

      -- crear registro histórico
      INSERT INTO his_AUT_USOSUBUSO_PROD(
CCLAVEH,
CTIPVEH,
CUSO,
CSUBUSO,
SPRODUC,
CACTIVI,
CUSUALT,
FALTA,
CUSUMOD,
FMODIFI,
FBAJA,
CEMPRES,
CRETEN,
CUSUHIST,FCREAHIST,ACCION)
VALUES(
:OLD.CCLAVEH,
:OLD.CTIPVEH,
:OLD.CUSO,
:OLD.CSUBUSO,
:OLD.SPRODUC,
:OLD.CACTIVI,
:OLD.CUSUALT,
:OLD.FALTA,
:OLD.CUSUMOD,
:OLD.FMODIFI,
:OLD.FBAJA,
:OLD.CEMPRES,
:OLD.CRETEN,
f_user, f_sysdate, ''||vaccion||'');
EXCEPTION
   WHEN OTHERS THEN
      p_tab_error(f_sysdate, f_user, 'TRIGGER trg_his_AUT_USOSUBUSO_PROD', 1, SQLCODE, SQLERRM);
      RAISE;
END trg_his_AUT_USOSUBUSO_PROD;





/
ALTER TRIGGER "AXIS"."TRG_HIS_AUT_USOSUBUSO_PROD" ENABLE;
