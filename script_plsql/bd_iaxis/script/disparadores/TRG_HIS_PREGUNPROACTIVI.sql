--------------------------------------------------------
--  DDL for Trigger TRG_HIS_PREGUNPROACTIVI
--------------------------------------------------------

  CREATE OR REPLACE EDITIONABLE TRIGGER "AXIS"."TRG_HIS_PREGUNPROACTIVI" 
   BEFORE UPDATE OR DELETE
   ON PREGUNPROACTIVI
   FOR EACH ROW
   DECLARE
       vaccion VARCHAR2(2);
BEGIN
   IF UPDATING THEN
       vaccion := 'U';
   ELSE
       vaccion := 'D';
   END IF;

      -- crear registro histórico
      INSERT INTO his_PREGUNPROACTIVI(
CRAMO,
CMODALI,
CCOLECT,
CTIPSEG,
CACTIVI,
CPREGUN,
SPRODUC,
CPRETIP,
NPREORD,
TPREFOR,
CPREOBL,
NPREIMP,
CRESDEF,
COFERSN,
TVALFOR,
CUSUALT,
FALTA,
CUSUMOD,
FMODIFI,
CUSUHIST,FCREAHIST,ACCION)
VALUES(
:OLD.CRAMO,
:OLD.CMODALI,
:OLD.CCOLECT,
:OLD.CTIPSEG,
:OLD.CACTIVI,
:OLD.CPREGUN,
:OLD.SPRODUC,
:OLD.CPRETIP,
:OLD.NPREORD,
:OLD.TPREFOR,
:OLD.CPREOBL,
:OLD.NPREIMP,
:OLD.CRESDEF,
:OLD.COFERSN,
:OLD.TVALFOR,
:OLD.CUSUALT,
:OLD.FALTA,
:OLD.CUSUMOD,
:OLD.FMODIFI,
f_user, f_sysdate, ''||vaccion||'');
EXCEPTION
   WHEN OTHERS THEN
      p_tab_error(f_sysdate, f_user, 'TRIGGER trg_his_PREGUNPROACTIVI', 1, SQLCODE, SQLERRM);
      RAISE;
END trg_his_PREGUNPROACTIVI;





/
ALTER TRIGGER "AXIS"."TRG_HIS_PREGUNPROACTIVI" ENABLE;
