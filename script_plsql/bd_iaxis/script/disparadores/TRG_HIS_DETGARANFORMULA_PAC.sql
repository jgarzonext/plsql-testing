--------------------------------------------------------
--  DDL for Trigger TRG_HIS_DETGARANFORMULA_PAC
--------------------------------------------------------

  CREATE OR REPLACE EDITIONABLE TRIGGER "AXIS"."TRG_HIS_DETGARANFORMULA_PAC" 
   BEFORE UPDATE OR DELETE
   ON DETGARANFORMULA_PAC
   FOR EACH ROW
   DECLARE
       vaccion VARCHAR2(2);
BEGIN
   IF UPDATING THEN
       vaccion := 'U';
   ELSE
       vaccion := 'D';
   END IF;

      -- crear registro histórico
      INSERT INTO his_DETGARANFORMULA_PAC(
SPRODUC,
CGARANT,
CCONCEP,
CGRABA,
CTRATA,
VALORDEF,
NORDEN,
CUSUALT,
FALTA,
CUSUMOD,
FMODIFI,
CUSUHIST,FCREAHIST,ACCION)
VALUES(
:OLD.SPRODUC,
:OLD.CGARANT,
:OLD.CCONCEP,
:OLD.CGRABA,
:OLD.CTRATA,
:OLD.VALORDEF,
:OLD.NORDEN,
:OLD.CUSUALT,
:OLD.FALTA,
:OLD.CUSUMOD,
:OLD.FMODIFI,
f_user, f_sysdate, ''||vaccion||'');
EXCEPTION
   WHEN OTHERS THEN
      p_tab_error(f_sysdate, f_user, 'TRIGGER trg_his_DETGARANFORMULA_PAC', 1, SQLCODE, SQLERRM);
      RAISE;
END trg_his_DETGARANFORMULA_PAC;





/
ALTER TRIGGER "AXIS"."TRG_HIS_DETGARANFORMULA_PAC" ENABLE;
