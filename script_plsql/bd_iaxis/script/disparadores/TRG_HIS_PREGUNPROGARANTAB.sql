--------------------------------------------------------
--  DDL for Trigger TRG_HIS_PREGUNPROGARANTAB
--------------------------------------------------------

  CREATE OR REPLACE EDITIONABLE TRIGGER "AXIS"."TRG_HIS_PREGUNPROGARANTAB" 
   BEFORE UPDATE OR DELETE
   ON PREGUNPROGARANTAB
   FOR EACH ROW
   DECLARE
       vaccion VARCHAR2(2);
BEGIN
   IF UPDATING THEN
       vaccion := 'U';
   ELSE
       vaccion := 'D';
   END IF;

      -- crear registro histórico
      INSERT INTO his_PREGUNPROGARANTAB(
CPREGUN,
SPRODUC,
CACTIVI,
CGARANT,
COLUMNA,
TVALIDA,
CTIPDATO,
COBLIGA,
CREVALORIZA,
CUSUALT,
FALTA,
CUSUMOD,
FMODIFI,
CUSUHIST,FCREAHIST,ACCION)
VALUES(
:OLD.CPREGUN,
:OLD.SPRODUC,
:OLD.CACTIVI,
:OLD.CGARANT,
:OLD.COLUMNA,
:OLD.TVALIDA,
:OLD.CTIPDATO,
:OLD.COBLIGA,
:OLD.CREVALORIZA,
:OLD.CUSUALT,
:OLD.FALTA,
:OLD.CUSUMOD,
:OLD.FMODIFI,
f_user, f_sysdate, ''||vaccion||'');
EXCEPTION
   WHEN OTHERS THEN
      p_tab_error(f_sysdate, f_user, 'TRIGGER trg_his_PREGUNPROGARANTAB', 1, SQLCODE, SQLERRM);
      RAISE;
END trg_his_PREGUNPROGARANTAB;





/
ALTER TRIGGER "AXIS"."TRG_HIS_PREGUNPROGARANTAB" ENABLE;
