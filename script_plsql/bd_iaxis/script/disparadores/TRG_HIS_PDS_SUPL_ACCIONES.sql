--------------------------------------------------------
--  DDL for Trigger TRG_HIS_PDS_SUPL_ACCIONES
--------------------------------------------------------

  CREATE OR REPLACE EDITIONABLE TRIGGER "AXIS"."TRG_HIS_PDS_SUPL_ACCIONES" 
   BEFORE UPDATE OR DELETE
   ON PDS_SUPL_ACCIONES
   FOR EACH ROW
   DECLARE
       vaccion VARCHAR2(2);
BEGIN
   IF UPDATING THEN
       vaccion := 'U';
   ELSE
       vaccion := 'D';
   END IF;

      -- crear registro histórico
      INSERT INTO his_PDS_SUPL_ACCIONES(
CMOTMOV,
SPRODUC,
NORDEN,
ORDENACC,
DINACCION,
TTABLE,
TCAMPO,
TWHERE,
TACCION,
NACCION,
VACCION,
TTARIFA,
CUSUALT,
FALTA,
CUSUMOD,
FMODIFI,
CUSUHIST,FCREAHIST,ACCION)
VALUES(
:OLD.CMOTMOV,
:OLD.SPRODUC,
:OLD.NORDEN,
:OLD.ORDENACC,
:OLD.DINACCION,
:OLD.TTABLE,
:OLD.TCAMPO,
:OLD.TWHERE,
:OLD.TACCION,
:OLD.NACCION,
:OLD.VACCION,
:OLD.TTARIFA,
:OLD.CUSUALT,
:OLD.FALTA,
:OLD.CUSUMOD,
:OLD.FMODIFI,
f_user, f_sysdate, ''||vaccion||'');
EXCEPTION
   WHEN OTHERS THEN
      p_tab_error(f_sysdate, f_user, 'TRIGGER trg_his_PDS_SUPL_ACCIONES', 1, SQLCODE, SQLERRM);
      RAISE;
END trg_his_PDS_SUPL_ACCIONES;





/
ALTER TRIGGER "AXIS"."TRG_HIS_PDS_SUPL_ACCIONES" ENABLE;
