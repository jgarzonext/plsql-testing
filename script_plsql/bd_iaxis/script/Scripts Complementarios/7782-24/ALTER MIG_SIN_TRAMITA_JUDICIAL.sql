BEGIN
  EXECUTE IMMEDIATE 'ALTER TABLE MIG_SIN_TRAMITA_JUDICIAL DROP COLUMN CORALPROC'; 
EXCEPTION
  WHEN OTHERS THEN
    IF (SQLCODE = -00904) THEN
         DBMS_OUTPUT.PUT_LINE('La columna CORALPROC no existe en la tabla. Ignorar este mensaje cuando se ejecute el lanzador varias veces.');
    ELSE
     DBMS_OUTPUT.PUT_LINE('ERROR...' || '. Descripción del Error-> ' || SQLERRM || ' - ' || DBMS_UTILITY.FORMAT_ERROR_BACKTRACE);
    END IF;
END;
/
BEGIN
  EXECUTE IMMEDIATE 'ALTER TABLE MIG_SIN_TRAMITA_JUDICIAL DROP COLUMN UNICAINST';
EXCEPTION
  WHEN OTHERS THEN
    IF (SQLCODE = -00904) THEN
         DBMS_OUTPUT.PUT_LINE('La columna UNICAINST no existe en la tabla. Ignorar este mensaje cuando se ejecute el lanzador varias veces.');
    ELSE
     DBMS_OUTPUT.PUT_LINE('ERROR...' || '. Descripción del Error-> ' || SQLERRM || ' - ' || DBMS_UTILITY.FORMAT_ERROR_BACKTRACE);
    END IF;
END;
/
BEGIN
  EXECUTE IMMEDIATE 'ALTER TABLE MIG_SIN_TRAMITA_JUDICIAL DROP COLUMN FUNICAINST'; 
EXCEPTION
  WHEN OTHERS THEN
    IF (SQLCODE = -00904) THEN
         DBMS_OUTPUT.PUT_LINE('La columna FUNICAINST no existe en la tabla. Ignorar este mensaje cuando se ejecute el lanzador varias veces.');
    ELSE
     DBMS_OUTPUT.PUT_LINE('ERROR...' || '. Descripción del Error-> ' || SQLERRM || ' - ' || DBMS_UTILITY.FORMAT_ERROR_BACKTRACE);
    END IF;
END;
/
ALTER TABLE MIG_SIN_TRAMITA_JUDICIAL ADD CORALPROC NUMBER(2);
ALTER TABLE MIG_SIN_TRAMITA_JUDICIAL ADD UNICAINST NUMBER(1);
ALTER TABLE MIG_SIN_TRAMITA_JUDICIAL ADD FUNICAINST DATE;
/
BEGIN
  EXECUTE IMMEDIATE 'ALTER TABLE MIG_SIN_TRAMITA_JUDICIAL_UAT DROP COLUMN CORALPROC'; 
EXCEPTION
  WHEN OTHERS THEN
    IF (SQLCODE = -00904) THEN
         DBMS_OUTPUT.PUT_LINE('La columna CORALPROC no existe en la tabla. Ignorar este mensaje cuando se ejecute el lanzador varias veces.');
    ELSE
     DBMS_OUTPUT.PUT_LINE('ERROR...' || '. Descripción del Error-> ' || SQLERRM || ' - ' || DBMS_UTILITY.FORMAT_ERROR_BACKTRACE);
    END IF;
END;
/
BEGIN
  EXECUTE IMMEDIATE 'ALTER TABLE MIG_SIN_TRAMITA_JUDICIAL_UAT DROP COLUMN UNICAINST';
EXCEPTION
  WHEN OTHERS THEN
    IF (SQLCODE = -00904) THEN
         DBMS_OUTPUT.PUT_LINE('La columna UNICAINST no existe en la tabla. Ignorar este mensaje cuando se ejecute el lanzador varias veces.');
    ELSE
     DBMS_OUTPUT.PUT_LINE('ERROR...' || '. Descripción del Error-> ' || SQLERRM || ' - ' || DBMS_UTILITY.FORMAT_ERROR_BACKTRACE);
    END IF;
END;
/
BEGIN
  EXECUTE IMMEDIATE 'ALTER TABLE MIG_SIN_TRAMITA_JUDICIAL_UAT DROP COLUMN FUNICAINST'; 
EXCEPTION
  WHEN OTHERS THEN
    IF (SQLCODE = -00904) THEN
         DBMS_OUTPUT.PUT_LINE('La columna FUNICAINST no existe en la tabla. Ignorar este mensaje cuando se ejecute el lanzador varias veces.');
    ELSE
     DBMS_OUTPUT.PUT_LINE('ERROR...' || '. Descripción del Error-> ' || SQLERRM || ' - ' || DBMS_UTILITY.FORMAT_ERROR_BACKTRACE);
    END IF;
END;
/
ALTER TABLE MIG_SIN_TRAMITA_JUDICIAL_UAT ADD CORALPROC NUMBER(2);
ALTER TABLE MIG_SIN_TRAMITA_JUDICIAL_UAT ADD UNICAINST NUMBER(1);
ALTER TABLE MIG_SIN_TRAMITA_JUDICIAL_UAT ADD FUNICAINST DATE;