EXEC PAC_SKIP_ORA.P_COMPROVADROP('TEXTO_JSPR','TABLE'); 
CREATE TABLE "TEXTO_JSPR"
  (
    "ID"       NUMBER(2,0) NOT NULL ENABLE,
    "CONCEPTO" VARCHAR2(10 BYTE) NOT NULL ENABLE,
    "TEXTO"    VARCHAR2(4000 BYTE) NOT NULL ENABLE,
    CONSTRAINT "TEXTO_JSPR_PK" PRIMARY KEY ("ID") 
 )
;

EXEC PAC_SKIP_ORA.P_COMPROVADROP('PRODUCTO_TEXTO_JSPR','TABLE'); 
CREATE TABLE "PRODUCTO_TEXTO_JSPR"
  (
    "SPRODUC"  NUMBER(6,0) NOT NULL ENABLE,
    "CACTIVI"  NUMBER(4,0) NOT NULL ENABLE,
    "ID_TEXTO" NUMBER(2,0) NOT NULL ENABLE,
    "ORDEN"    NUMBER(2,0) NOT NULL ENABLE,
    CONSTRAINT "PRODUCTO_TEXTO_JSPR_PK" PRIMARY KEY ("SPRODUC", "CACTIVI", "ID_TEXTO", "ORDEN"),
    FOREIGN KEY ("ID_TEXTO") REFERENCES "TEXTO_JSPR" ("ID") ENABLE );


EXEC PAC_SKIP_ORA.P_COMPROVADROP ('FORMATO_PLANILLA','TABLE'); 
CREATE TABLE "FORMATO_PLANILLA"
  (
    "SPRODUC"  NUMBER NOT NULL ENABLE,
    "CACTIVI"  NUMBER NOT NULL ENABLE,
    "FORMATO" VARCHAR2(20 BYTE) NOT NULL ENABLE,
    CONSTRAINT "FORMATO_PLANILLA_PK" PRIMARY KEY ("SPRODUC", "CACTIVI"));
/
