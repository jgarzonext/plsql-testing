CREATE TABLE OBS_CUENTACOBRO
(
  COBSERVACION NUMBER NOT NULL,
  SSEGURO NUMBER NOT NULL,
  NRECIBO NUMBER NOT NULL,
  OBSERVACION VARCHAR2(200),
  CONSTRAINT OBS_CUENTACOBRO_PK PRIMARY KEY (COBSERVACION )ENABLE );

ALTER TABLE OBS_CUENTACOBRO
ADD CONSTRAINT FK_RECIBOS FOREIGN KEY(NRECIBO ) REFERENCES RECIBOS( NRECIBO )ENABLE;

ALTER TABLE OBS_CUENTACOBRO
ADD CONSTRAINT FK_SSEGUROS FOREIGN KEY( SSEGURO ) REFERENCES SEGUROS( SSEGURO )ENABLE;

create or replace synonym AXIS00.OBS_CUENTACOBRO for AXIS.OBS_CUENTACOBRO; 

GRANT SELECT, INSERT, UPDATE, DELETE ON  OBS_CUENTACOBRO TO r_axis;

commit;
/